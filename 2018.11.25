####今天学到的
   ## #ifndef指令
        通常包括多个头文件时，其中的文件可能包括了相同的宏定义。#ifndef指令可以防止相同的宏
        被重复定义。在首次定义一个宏的头文件中用#ifndef指令激活定义，随后在其他头文件中的定
        义都被忽略 
   ##  #ifndef指令判断后面的标识符是否时未定义的，常用于定义之前未定义的常量
  ...c
    /* arrays.h */
  
 #ifndef SIZE	
	#define SIZE 100  
  #endif 
 ...   
